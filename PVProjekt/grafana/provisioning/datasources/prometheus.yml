# Tells Grafana about the Prometheus data source on startup,
# so you don't have to add it by hand in the UI every time.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true