# How often Prometheus scrapes targets and what it scrapes.
global:
  scrape_interval: 5s          # matches the PV data cadence (~every 5 s)

scrape_configs:
  - job_name: pv_collector
    static_configs:
      - targets: ["collector:8000"]   # "collector" = the service name below